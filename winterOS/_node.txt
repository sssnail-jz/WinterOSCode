1，唤醒 TTY 的不再是 key_pressed ，而是 kb_in.cont > 0就唤醒！

2，当前的 kb_in buffer size 是 3，因为就算是有 6 个扫描码组成一个字符，但是
    因为扫描码是每次中断送一个，所以应该有充足的余地获取到 6 个扫描码

3，在 echo 执行的过程中键盘中断可使奔溃 √

4，修改了 sys_receive 函数，其中对中断等待的代码：
    // 想从具体进程接收
	else if(src != INTERRUPT)

5，控制台换行太快有 assert(fs_msg.CNT == len) 错误

6，两次重启发送的消息数不一样

7，读取硬盘是以扇区为单位读的，请求的任务也应当以扇区为单位请求，并且起始位置也是扇区对齐

8，untar 之後要刪除 tar 文件，否則文件系統分配至 tar 文件時會亂掉 ***（但应该不会，如果起点是 2048 ）

9，需要写一个 seek ，要不然很不方便

10，do_unlink 的最后两个部分没有注释

/////////

11，ls 要改一下，功能分离 获取所有文件信息 + 怎样处理文件信息

12，do_open 混合创建和读写标志混合的情况